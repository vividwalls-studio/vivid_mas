version: '3.8'

services:
  analytics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vividwalls-analytics
    restart: unless-stopped
    ports:
      - "8114:8114"
    environment:
      - NODE_ENV=production
      - MCP_TRANSPORT=http
      - HTTP_PORT=8114
      - HTTP_HOST=0.0.0.0
      - PORT=8114
      - HOST=0.0.0.0
      - LOG_LEVEL=info
    networks:
      - vivid_mas
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8114/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "com.vividwalls.service=analytics"
      - "com.vividwalls.type=mcp-server"
      - "com.vividwalls.transport=http+sse"

networks:
  vivid_mas:
    external: true
