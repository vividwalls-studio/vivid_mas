version: '3.8'

services:
  copy-writer-resource:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vividwalls-copy-writer-resource
    restart: unless-stopped
    ports:
      - "8142:8142"
    environment:
      - NODE_ENV=production
      - TRANSPORT=http
      - PORT=8142
      - HOST=0.0.0.0
      - LOG_LEVEL=info
    networks:
      - vivid_mas
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8142/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "com.vividwalls.service=copy-writer-resource"
      - "com.vividwalls.type=mcp-server"
      - "com.vividwalls.transport=streamable-http"

networks:
  vivid_mas:
    external: true
