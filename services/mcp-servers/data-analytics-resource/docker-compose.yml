version: '3.8'

services:
  data-analytics-resource:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vividwalls-data-analytics-resource
    restart: unless-stopped
    ports:
      - "8143:8143"
    environment:
      - NODE_ENV=production
      - MCP_TRANSPORT=http
      - HTTP_PORT=8143
      - HTTP_HOST=0.0.0.0
      - PORT=8143
      - HOST=0.0.0.0
      - LOG_LEVEL=info
    networks:
      - vivid_mas
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8143/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "com.vividwalls.service=data-analytics-resource"
      - "com.vividwalls.type=mcp-server"
      - "com.vividwalls.transport=http+sse"

networks:
  vivid_mas:
    external: true
