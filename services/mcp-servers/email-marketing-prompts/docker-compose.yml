version: '3.8'

services:
  email-marketing-prompts:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vividwalls-email-marketing-prompts
    restart: unless-stopped
    ports:
      - "8134:8134"
    environment:
      - NODE_ENV=production
      - MCP_TRANSPORT=http
      - HTTP_PORT=8134
      - HTTP_HOST=0.0.0.0
      - PORT=8134
      - HOST=0.0.0.0
      - LOG_LEVEL=info
    networks:
      - vivid_mas
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8134/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "com.vividwalls.service=email-marketing-prompts"
      - "com.vividwalls.type=mcp-server"
      - "com.vividwalls.transport=http+sse"

networks:
  vivid_mas:
    external: true
