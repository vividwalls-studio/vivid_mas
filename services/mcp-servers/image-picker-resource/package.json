{
  "name": "image-picker-resource",
  "version": "0.1.0",
  "description": "MCP resource server to pick product images from Supabase",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "pretest": "npm run build",
    "test": "node test/test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
