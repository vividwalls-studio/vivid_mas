version: '3.8'

services:
  marketing-research-resource:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vividwalls-marketing-research-resource
    restart: unless-stopped
    ports:
      - "8147:8147"
    environment:
      - NODE_ENV=production
      - MCP_TRANSPORT=http
      - HTTP_PORT=8147
      - HTTP_HOST=0.0.0.0
      - PORT=8147
      - HOST=0.0.0.0
      - LOG_LEVEL=info
    networks:
      - vivid_mas
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8147/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "com.vividwalls.service=marketing-research-resource"
      - "com.vividwalls.type=mcp-server"
      - "com.vividwalls.transport=http+sse"

networks:
  vivid_mas:
    external: true
