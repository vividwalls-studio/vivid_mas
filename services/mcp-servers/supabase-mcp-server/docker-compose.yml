version: '3.8'

services:
  supabase-mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vividwalls-supabase-mcp-server
    restart: unless-stopped
    ports:
      - "4043:4043"
    env_file:
      - .env
    environment:
      - MCP_TRANSPORT=http
      - NODE_ENV=production
      - PORT=4043
      - HOST=0.0.0.0
      - LOG_LEVEL=info
    networks:
      - vivid_mas
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:4043/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    labels:
      - "com.vividwalls.service=supabase-mcp-server"
      - "com.vividwalls.type=mcp-server"
      - "com.vividwalls.transport=http+sse"

networks:
  vivid_mas:
    external: true
